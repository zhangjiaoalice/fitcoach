# === FastAPI 核心 ===
fastapi==0.115.6
uvicorn[standard]==0.34.0

# === 数据层：SQLAlchemy 2.0 异步 + PostgreSQL 驱动 + 迁移工具 ===
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0            # 异步 PostgreSQL 驱动
alembic==1.14.0           # 数据库迁移

# === 配置与校验 ===
pydantic==2.10.4
pydantic-settings==2.7.0  # 从 .env 读配置

# === 认证 (M1 用) ===
passlib[bcrypt]==1.7.4    # 密码哈希
pyjwt==2.10.1             # JWT 签发/校验
python-multipart==0.0.20  # 表单/文件

# === LLM (M4 用) ===
openai==1.59.5            # Moonshot 兼容 OpenAI SDK

# === 测试 (贯穿全程) ===
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1             # 测试用异步 HTTP 客户端
